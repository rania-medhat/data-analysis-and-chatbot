# Copy this file to `.env` (for local dev only) and set values accordingly.
# WARNING: Never commit your real API keys to GitHub. Use project secrets in your hosting provider.

# Frontend (Vite) - only for local dev if you need it (not required for server-side calls)
VITE_GEMINI_API_KEY=
VITE_OPENAI_API_KEY=

# Server / Supabase Edge Function (must be set in your server runtime provider, not the client)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Supabase (used server-side and frontend config helper)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# Other optional envs
NODE_ENV=development

# Example: set GEMINI_API_KEY to a key starting with AIza... (from https://aistudio.google.com/app/apikey)
